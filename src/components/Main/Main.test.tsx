import {render, screen} from '@testing-library/react';
import React from 'react';
import Main from './Main';

describe ('Main component Routes testing', () => {
    it('the demo test', () => {
        expect(42).toEqual(42);
    })
});